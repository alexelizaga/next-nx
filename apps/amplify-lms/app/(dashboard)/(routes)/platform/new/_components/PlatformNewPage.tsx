'use client';

import { Heading } from '@aws-amplify/ui-react';

const PlatformNewPage = () => {
  return (
    <div>
      <Heading level={1}>Platform New</Heading>
    </div>
  );
};

export default PlatformNewPage;
